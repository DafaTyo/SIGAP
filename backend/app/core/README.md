"""Core configuration and logger for SIGAP backend.

- `config.py` loads environment variables via `pydantic-settings`.
- `logger.py` provides a JSON‑structured logger (structlog).
- `exceptions.py` defines a base SIGAPException that other modules can subclass.
"""
