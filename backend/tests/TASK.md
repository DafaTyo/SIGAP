# TASK.md – Backend/tests

## Goals
- Unit testing untuk semua modul backend (api, models, services, policies).
- Integrasi testing dengan test client FastAPI.

## Verification Criteria
- [] Semua test lulus (`pytest -q`).
- [] Coverage ≥ 90 % pada paket `app/`.
- [] CI pipeline gagal bila coverage < 90 %.

## Status
- [] Pending