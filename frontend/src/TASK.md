# 📌 Module Task Tracker: Frontend Source Root (frontend/src)

## 🎯 Core Objective & Responsibility
- Sebagai **root source** untuk semua komponen, hook, action, utilitas, style, dan test di aplikasi Next.js.
- Memastikan struktur folder memudahkan *tree‑shaking* dan import yang jelas.

## 📋 Development Roadmap / Checklist
- [ ] **Create placeholder `index.ts`** (export barrel untuk memudahkan re‑export).
- [ ] **Add README** – deskripsi singkat tiap sub‑folder (components, hooks, actions, utils, styles, tests).
- [ ] **Enforce naming convention** – semua file .tsx/.ts mengikuti kebab-case.
- [ ] **Setup linting** – placeholder `.eslintrc.json` dan `prettier.config.js` (tidak kode, hanya config).

## 🔒 Constraints 
- Tidak menambahkan kode aplikasi (komponen, hook, dll.) sampai checklist root selesai.
- Semua folder harus memiliki `README.md` yang menjelaskan tujuan.

## 📄 References
- `docs/DESIGN.md` – UI component hierarchy.
