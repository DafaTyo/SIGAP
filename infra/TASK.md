# TASK.md (Infra)

- **Goals**:
  - Define Docker Compose, CI/CD pipelines, monitoring, and deployment scripts.
- **Verification Criteria**:
  - [] Docker Compose brings up all services (`docker compose up -d`).
  - [] CI workflow runs lint, tests, and builds Docker images.
  - [] Prometheus/Grafana dashboards are provisioned.
- **Status**: []
